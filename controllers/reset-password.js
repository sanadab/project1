var express = require("express");
var mongoose = require("mongoose");
const app = express();

app.route(async, "/reset-password", (request, response))
{
    try{
        var requestBody = this.request.body;
    }catch(err)
    {
        
    }
}


module.exports = app;